server:
  port: 9003
spring:
  application:
    name: GeneratePensionerDetails
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/
    fetch-registry: true
    register-with-eureka: true
