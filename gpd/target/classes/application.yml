server:
  port: 9001
spring:
  application:
    name: GeneratePensionerDetails
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9002/eureka
    fetch-registry: true
    register-with-eureka: true